 import numpy as np
m=[[4,5],[8,1],[1,3]]
m2=np.array(m)
#we will first change the list to array then we will apply the
# reshape module bez reshanpe attribute is for array  
m1=m2.reshape(1,6)
print(m1)
